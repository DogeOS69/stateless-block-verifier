# DogeOS Testdata

Store DogeOS-only witness fixtures here so they do not get mixed into
upstream-owned fixture directories.

Suggested layout:

- `testdata/dogeos/<feature>/<fork-or-network>/...json`

Each feature directory should include a short note describing where the data
came from and how to regenerate it.
